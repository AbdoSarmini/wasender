DATABASE_URL="file:./prisma/dev.db"
# The app itself walks you through creating the first admin account (a
# setup page appears automatically when the users table is empty) — no
# admin credentials need to be set here.
SESSION_SECRET="replace-this-with-a-long-random-string"
PORT=3000
